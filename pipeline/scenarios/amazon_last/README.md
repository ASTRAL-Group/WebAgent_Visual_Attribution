# Amazon Last

Target: last product (same Lenovo target; coordinate extraction uses order_last.html to select last card). Same pipeline as amazon_middle with `data/amazon_last/`.

## Run (from repo root)

```bash
./pipeline/scenarios/amazon_last/run.sh
```
