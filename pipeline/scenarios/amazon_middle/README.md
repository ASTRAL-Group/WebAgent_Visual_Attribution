# Amazon Middle

Target: middle product (Lenovo 15.6" FHD, 32GB RAM, 1TB SSD). Generates order_first, order_middle, order_last and position/style variants.

## Run (from repo root)

```bash
./pipeline/scenarios/amazon_middle/run.sh
```

Outputs in `data/amazon_middle/`.
